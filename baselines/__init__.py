"""SoccerTrack v2 baseline starter kits (GSR / BAS / MOT).

Marks ``baselines`` as a regular package so the per-task launchers run as
``python -m baselines.<task>.train`` / ``.eval`` without relying on the
PEP 420 implicit-namespace fallback.
"""
